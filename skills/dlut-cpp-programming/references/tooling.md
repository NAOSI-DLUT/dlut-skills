# 工具链

- 单文件可用 `g++ file.cpp -std=c++17 -Wall -Wextra -o file`。
- Visual Studio 下注意项目字符集、入口文件和标准版本。
- 调试类题时优先写最小 `main()` 构造对象并调用所有公开方法。

## 常见编译问题

- 成员函数声明和定义签名不一致。
- 忘记 `const` 导致无法调用。
- 运算符重载返回类型不合适。
- 动态数组拷贝浅拷贝导致重复释放。
